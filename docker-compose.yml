services:
  postgres:
    image: postgres:17
    container_name: ticketing-db
    restart: always
    environment:
      POSTGRES_DB: ticketing
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"